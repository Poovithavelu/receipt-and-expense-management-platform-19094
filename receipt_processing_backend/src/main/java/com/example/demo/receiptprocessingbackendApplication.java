package com.example.demo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Entry point for the Receipt Processing Backend Spring Boot application.
 * Renamed to follow Java class naming conventions and to align package with directory structure.
 */
@SpringBootApplication
public class ReceiptProcessingBackendApplication {

	public static void main(String[] args) {
		SpringApplication.run(ReceiptProcessingBackendApplication.class, args);
	}
}
